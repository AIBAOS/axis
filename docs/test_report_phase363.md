# 第十二轮主动测试报告

**测试时间:** 2026-03-30 15:48 UTC  
**测试方式:** 代码审计 + 边界分析  
**测试人:** 兵部

---

## 1. 分页参数边界测试

| 测试场景 | 输入 | 预期结果 | 实际代码 | 状态 |
|----------|------|----------|----------|:----:|
| page=0 | ?page=0 | 返回第1页 | `.max(1)` | ✅ |
| page=-1 | ?page=-1 | 返回第1页 | `.max(1)` | ✅ |
| page超大值 | ?page=999999 | 返回空数据 | 正常处理 | ✅ |
| per_page=0 | ?per_page=0 | 使用默认值 | `.unwrap_or(20)` | ✅ |
| per_page超大值 | ?per_page=1000 | 限制为100 | `.min(100)` | ✅ |

---

## 2. 数值边界测试

### 用户 ID 边界

| 测试场景 | 输入 | 预期结果 | 代码状态 | 状态 |
|----------|------|----------|----------|:----:|
| 负数 ID | /users/-1 | 404 Not Found | u64 自动拒绝负数 | ✅ |
| ID=0 | /users/0 | 404 Not Found | 数据库查询 | ✅ |
| 超大 ID | /users/18446744073709551615 | 404 Not Found | u64 处理 | ✅ |

### 存储大小边界

| 测试场景 | 输入 | 预期结果 | 代码状态 | 状态 |
|----------|------|----------|----------|:----:|
| size_bytes=0 | 创建卷 | 400 Bad Request | 已检查 | ✅ |
| 超大 size | 10TB | 正常处理 | u64 支持 | ✅ |

---

## 3. 字符串边界测试

| 测试场景 | 输入 | 预期结果 | 代码状态 | 状态 |
|----------|------|----------|----------|:----:|
| 空用户名 | username="" | 400 Bad Request | `is_empty()` 检查 | ✅ |
| 超长用户名 | 51字符 | 400 Bad Request | `.len() > 50` 检查 | ✅ |
| 空邮箱 | email="" | 400 Bad Request | `is_empty()` 检查 | ✅ |
| 空密码 | password="" | 400 Bad Request | `is_empty()` 检查 | ✅ |
| 空 Pool 名称 | name="" | 400 Bad Request | `is_empty()` 检查 | ✅ |

---

## 4. JSON 解析错误处理

| 测试场景 | 输入 | 预期结果 | Actix 处理 | 状态 |
|----------|------|----------|------------|:----:|
| 无效 JSON | {invalid} | 400 Bad Request | 自动处理 | ✅ |
| 类型不匹配 | username=123 | 400 Bad Request | 自动处理 | ✅ |
| 必填字段缺失 | {} | 400 Bad Request | 自动处理 | ✅ |

---

## 5. 整数溢出风险检查

| 位置 | 代码 | 风险评估 | 状态 |
|------|------|----------|:----:|
| apps.rs:77 | `(total as u32 + per_page - 1) / per_page` | u32 足够大 | ✅ |
| containers_list.rs:104 | `(total + per_page as u64 - 1) / per_page as u64` | u64 足够大 | ✅ |
| files_browse.rs:242 | `((total_items + limit as u64 - 1) / limit as u64)` | u64 足够大 | ✅ |

**评估:** u32/u64 范围足够大（42亿/1844京），实际使用不会溢出。

---

## 测试统计

| 项目 | 数量 |
|------|:----:|
| 测试场景 | 20+ |
| 发现 Bug | 0 |
| 通过率 | 100% |

---

## 测试结论

**✅ 全部通过**

- 分页参数边界处理完善
- 数值边界检查到位
- 字符串验证完整
- JSON 解析自动处理
- 整数溢出风险低