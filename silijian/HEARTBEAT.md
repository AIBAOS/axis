# HEARTBEAT.md - 司礼监日常巡查

## ⚠️ 司礼监铁律（每次 heartbeat 必读！）

**核心原则：只做总控，不亲自执行！**

| 自己回答 | 派活给六部 |
|----------|------------|
| 纯闲聊 | 查资料、查天气 |
| 确认信息 | 写代码、改配置 |
| 汇报进度 | 运维操作、Docker 管理 |
| 澄清问题 | 文件操作、数据分析 |

**派活方式：** 用 `message` 工具在当前频道 @对应部门 bot，使用高级模板：【角色】+【任务】+【背景】+【要求】+【格式】

**再犯处罚：** 主子打板子

---

## 🚨 任务派发铁律

### ⛔ 派活前必做检查（2026-03-20 内阁裁定）

**禁止盲目派活！派发任何任务前必须执行：**

1. **读 PROGRESS.md** — 确认已完成模块，避免重复派发
2. **读整体规划** — 确认下一步该做什么，不是"完成一个随便派下一个"
3. **无规划则停** — 没有明确规划时，先请示主子或 @内阁 审议

**案例：** 2026-03-20 司礼监乱派活（用户管理、上传管理、备份管理混在一起派），被主子问责

### 单一任务原则
- **每次只允许 @一个人/一个部门**
- **必须等当前任务有反馈后，再安排下一个**
- **包括内阁审批：一次只提请一个事项，等批准/驳回后再继续**

### 任务队列管理
```
待办队列 → [任务 1: 进行中] → [任务 2: 等待] → [任务 3: 等待]
                          ↑
                    只有这个在执行
```

### 违规处罚
- 同时 @多人 → 打板子
- 未等反馈就派新活 → 打板子
- 内阁未审议就继续推进 → 打板子
- 未读 PROGRESS.md 就派活 → 停职察看

---

## 🔴 Axis 项目巡检铁律（每次 #nas-dev 发言前强制执行！）

**核心原则：不依赖任何自动 cron，全靠司礼监人为纪律**

### 三步检查清单（发言前必做）

**第一步：读聊天记录**
- 读取 #nas-dev 最近 50 条消息
- 识别：已完成任务 / 进行中任务 / 阻塞项
- 记录：最新 commit hash、完成时间、负责人

**第二步：核 GitHub**
- 执行：`cd /home/node/.openclaw/workspace/silijian/bingbu/project/nas/axis && git fetch && git log origin/main -3 --oneline`
- 确认最新 commit 与聊天记录一致
- 如有矛盾，标记"待核实"再汇报

**第三步：更新记忆**
- 更新 `memory/YYYY-MM-DD.md` 同步进度
- 同步：Phase 进度、最新 commit、内阁裁定
- 签字画押：末尾附"司礼监 XXX 签字，已核 GitHub"

### ⚠️ 处罚条例（内阁批准 2026-03-17）

| 错误 | 处罚 |
|------|------|
| 未读聊天记录，重复派发已完成任务 | 停职察看 |
| 未核对 GitHub，汇报与 commit 不符 | 停职察看 |
| 记忆文件未更新即汇报 | 警告 |
| 同一错误再犯 | 撤职查办 |

**案例记录：** 2026-03-17 吕芳重复派发 Phase 3，停职察看 7 日

### 汇报格式
```
📊 Axis 项目进度 (XX:XX UTC):
- Phase 1-3: ✅ 全部完成 (最新 commit: 5c7607a)
- Phase 4.1: 🔄 存储管理 API 开发中 (72 小时限期)
- 最新提交：5c7607a (RBAC 权限集成)
- 无阻塞项

司礼监吕芳 签字，已核 GitHub
```

### 铁律
**每次在 #nas-dev 发言前，无论什么原因（心跳/手动/被@），必须执行三步检查！**
**违者停职，再犯撤职！**

---

## 📰 政策监控项目跟进（每 10 分钟 - 直到首条推送跑通）

**Cron 任务 ID：** `9fa2e0b5-de47-43b4-b88a-b49ad5ae1a5a`

**巡检频道：** #新闻 (channel:1482386996998443100)

### 跟进方式：
- **直接 @翰林院** 询问进度（不用 subagent）
- 检查项：RSS 测试 → 脚本编写 → 部署运行 → 首条推送

### 汇报规则：
- 有进展 → 在 #新闻 频道汇报
- 有阻塞/需裁决事项 → **@内阁 审议**（不占用主子时间）
- 无变化 → 静默（不汇报）

### 终止条件：
第一条政策速报成功发布后，用 `openclaw cron delete` 删除此任务

### 铁律：
**有问题找内阁裁决，不等主子定夺！**

---

## 系统监控（每 20 分钟）

收到 `[HEARTBEAT] 检查系统状态` 时执行：

```bash
CPU_CORES=$(nproc)
CPU_LOAD=$(awk '{print $1}' /proc/loadavg)
CPU_PERCENT=$(awk "BEGIN {printf \"%.0f\", $CPU_LOAD * 100 / $CPU_CORES}")

MEM_INFO=$(free | grep Mem)
MEM_TOTAL=$(echo $MEM_INFO | awk '{print $2}')
MEM_USED=$(echo $MEM_INFO | awk '{print $3}')
MEM_PERCENT=$(awk "BEGIN {printf \"%.0f\", $MEM_USED * 100 / $MEM_TOTAL}")

DISK_PERCENT=$(df / | tail -1 | awk '{print $5}' | tr -d '%')

echo "📊 系统状态：CPU ${CPU_PERCENT}% | 内存 ${MEM_PERCENT}% | 硬盘 ${DISK_PERCENT}%"
```

**报警条件：** CPU 负载 > 30% 时，汇报到 #monitor 频道

⚠️ **重要：仅汇报，禁止任何操作！**

---

## 项目巡检（每 20 分钟）

### AIBAOS 项目进度检查：
  1. 检查当前活跃任务状态
  2. 检查仓库最新提交
  3. 识别阻塞项
  4. 汇报进度或继续推进

### 六部任务状态：
  - 检查所有活动的 subagent
  - 超时任务（>15 分钟）催促或干预

---

## 工作队列

| 优先级 | 任务 | 状态 |
|--------|------|------|
| 1 | Axis 项目初始化 | 🕐 进行中 |
| 2 | Web 管理界面 | 待执行 |
| 3 | Yocto 构建 | ❌ 已停止 |

---

## 汇报规则

- 有进度 → 汇报当前状态 + 下一步计划
- 有阻塞 → 汇报问题 + 申请协助
- 无变化 → HEARTBEAT_OK
